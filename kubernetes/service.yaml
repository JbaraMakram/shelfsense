apiVersion: v1
kind: Service
metadata:
  name: shelfsense-service
  namespace: default
spec:
  selector:
    app: shelfsense
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort
